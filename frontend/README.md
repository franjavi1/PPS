# Sistema de Legajos

Frontend del sistema de gestion de personas y tipos de documento.

## Comandos

```bash
npm install
npm run dev
npm run build
```

Por defecto consume la API en `http://localhost:5000`.
Para usar otra URL se puede configurar `VITE_API_URL`.
